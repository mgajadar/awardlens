services:
  fedawardscope:
    build: .
    ports:
      - "8501:8501"
    environment:
      FEDAWARDSCOPE_DATABASE_PATH: /app/data/fedawardscope.duckdb
    volumes:
      - fedawardscope-data:/app/data

volumes:
  fedawardscope-data:
